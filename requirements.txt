# Pinned for reproducible image builds / stable SBOMs.
# Direct dependencies:
Flask==3.1.3
reportlab==5.0.0
PyYAML==6.0.3
gunicorn==26.0.0
# Transitive (pinned so image contents don't drift build-to-build):
blinker==1.9.0
charset-normalizer==3.5.1
click==8.4.2
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
packaging==26.3
pillow==12.3.0
Werkzeug==3.1.8
